import Vue from "vue";
import {
    Button, Swipe, SwipeItem,
    Badge, Tabbar, TabbarItem,
    NavBar, Icon, Search,
    Uploader, Form, Field,
    Tab, Tabs, Toast,
    IndexBar, IndexAnchor, Cell,
    Empty, CellGroup, Popup,
    Divider, Tag, Collapse, CollapseItem
} from 'vant';
Vue.use(Button).use(Swipe)
    .use(SwipeItem).use(Badge)
    .use(Tabbar).use(TabbarItem)
    .use(NavBar).use(Icon)
    .use(Search).use(Uploader)
    .use(Form).use(Field)
    .use(Tab).use(Tabs)
    .use(Toast).use(IndexBar)
    .use(IndexAnchor).use(Cell)
    .use(Empty).use(CellGroup)
    .use(Popup).use(Divider)
    .use(Tag).use(Collapse)
    .use(CollapseItem)